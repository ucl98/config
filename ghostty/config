background-opacity = 0.95
background-blur-radius = 3
macos-titlebar-style = native
window-padding-x = 10
window-padding-y = 10
theme = catppuccin-mocha
font-size = 16
font-feature = -calt
clipboard-read=allow
clipboard-write=allow

# Window management settings
window-save-state = always
window-step-resize = true

# This ensures Ghostty remembers window size and position
window-theme = auto

# Optional: Set specific window size if you want a default
# window-width = 120
# window-height = 40

# Shell integration for proper cleanup and session management
shell-integration = zsh
confirm-close-surface = false
quit-after-last-window-closed = true

# Auto-start tmux on new terminal windows
command = tmux new-session -A -s main

